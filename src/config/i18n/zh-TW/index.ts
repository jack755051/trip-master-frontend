import { common } from "./common";
import { auth } from "./auth";
import { nav } from "./nav";
import { status } from "./status";
// home
import { home, trip } from "./home";

export const zhTW = {
  common,
  nav,
  home,
  trip,
  auth,
  status,
};
