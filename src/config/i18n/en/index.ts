import { common } from "./common";
import { home } from "./home";
import { auth } from "./auth";
import { nav } from "./nav";
import { status } from "./status";

export const en = {
  common,
  nav,
  home,
  auth,
  status,
};
